class ChatMessage{
  String messageContent;
  String messageType;
  String type;
  String seen;
  ChatMessage({required this.messageContent, required this.messageType,required this.type,required this.seen});
}